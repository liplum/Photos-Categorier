<Window
    x:Class="PhotosCategorier.Main.ClearDuplicatesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:loc="clr-namespace:PhotosCategorier.Properties"
    xmlns:local="clr-namespace:PhotosCategorier.Main"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Static loc:Resources.ClearDuplicates}"
    Height="150"
    MinWidth="350"
    Loaded="Window_Loaded"
    ResizeMode="NoResize"
    SizeToContent="Width"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="30,5" />
            </Style>
        </Grid.Resources>
        <Label
            Name="Counter"
            Grid.ColumnSpan="2"
            VerticalContentAlignment="Center"
            Content="{x:Static loc:Resources.IsScanning}"
            FontSize="20" />
        <ProgressBar
            Name="progress"
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Height="auto"
            Margin="5" />
        <Button
            Name="OK"
            Grid.Row="2"
            Click="OK_Click"
            Content="{x:Static loc:Resources.OK}"
            IsEnabled="False" />
        <Button
            Name="Cancel"
            Grid.Row="2"
            Grid.Column="2"
            Click="Cancel_Click"
            Content="{x:Static loc:Resources.Cancel}"
            IsDefault="True"
            IsEnabled="False" />
    </Grid>
</Window>
